import { FC } from "react"
import { StyledFooter } from "./Footer.style"

const Footer = ({ title, email, socialMedia }) => (
  <StyledFooter data-testid="Footer">Footer Component</StyledFooter>
)

export { Footer }
